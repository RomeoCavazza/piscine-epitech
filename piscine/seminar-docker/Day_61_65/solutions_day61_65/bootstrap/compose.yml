services:
  hello-world:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
  
  db:
    image: postgres:15.2-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: popeye

